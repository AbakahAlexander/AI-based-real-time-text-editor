# Maven Commands for Speech-to-Text Program

## Compile the Project
```bash
mvn clean compile
```

## Run the Program
```bash
mvn exec:java -Dexec.mainClass="text_editor.SpeechToText"
```

Note: Make sure you're in the root directory of the project when running these commands.
